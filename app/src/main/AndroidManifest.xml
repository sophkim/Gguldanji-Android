<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guru_1">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GURU_1">
        <activity android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HbdCompleteActivity" />
        <activity android:name=".MissingCompleteActivity" />
        <activity android:name=".MissingInfoActivity" />
        <activity android:name=".MissingActivity" />
        <activity android:name=".HbdInfoActivity" />
        <activity android:name=".HbdActivity" />
        <activity android:name=".WeddingCompleteActivity" />
        <activity android:name=".WeddingInfoActivity" />
        <activity android:name=".WeddingActivity" />
        <activity android:name=".MainActivity"/>
    </application>

</manifest>